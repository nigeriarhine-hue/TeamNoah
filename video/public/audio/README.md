Narration and music go here.

    voiceover.mp3    one continuous 57.00s file, 48 kHz, silent until 0:00.5
    music.mp3        optional bed

Then set the matching flag in `src/Video.tsx`. The script, the read direction and
the timing table are in `../../VOICEOVER.md`.
